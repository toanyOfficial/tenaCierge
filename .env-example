# Next.js 서버가 참조할 공통 환경 변수 예시입니다.
# 운영 환경에서는 적절한 값으로 교체한 뒤 .env 파일을 만들고 사용하세요.

# 1) 전체 연결 문자열을 사용하는 방법
# DATABASE_URL="mysql://user:password@127.0.0.1:3306/tenaCierge"

# 2) 개별 옵션으로 설정하는 방법 (DATABASE_URL을 주석 처리한 경우에 사용)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=tena_user
DB_PASSWORD=secret_password
DB_NAME=tenaCierge
